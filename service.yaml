apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: gomaxprocs
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: gcr.io/cloudylabs-public/gomaxprocs:0.1.3
            resources:
              requests:
                cpu: "1"
              limits:
                cpu: "1"
